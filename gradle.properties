# Project build settings ONLY — this file is TRACKED BY GIT.
#
# Never put credentials here: no mavenCentralUsername/Password, no
# signingInMemoryKey*, no tokens or passphrases. Those belong in
# ~/.gradle/gradle.properties (untracked, mode 600) — same filename, different
# directory, which is exactly how they get confused. See PUBLISHING.md.

org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
